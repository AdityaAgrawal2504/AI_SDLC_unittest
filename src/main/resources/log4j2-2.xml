<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="INFO">
    <Appenders>
        <!-- Console Appender for local development -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} [ReqID: %X{requestId}] - %msg%n"/>
        </Console>

        <!-- Rolling File Appender for persistent, structured JSON logs -->
        <RollingFile name="JsonFile"
                     fileName="logs/app.log.json"
                     filePattern="logs/app-%d{yyyy-MM-dd}-%i.log.json.gz">
            <JsonLayout complete="false" compact="true" eventEol="true">
                <KeyValuePair key="timestamp" value="$${date:yyyy-MM-dd'T'HH:mm:ss.SSS'Z'}"/>
                <KeyValuePair key="level" value="%p"/>
                <KeyValuePair key="logger" value="%c{1}"/>
                <KeyValuePair key="requestId" value="%X{requestId}"/>
                <KeyValuePair key="message" value="%m"/>
                <KeyValuePair key="thread" value="%t"/>
            </JsonLayout>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>

        <!-- Abstracted Kafka Appender for integration with external systems -->
        <!-- This is a placeholder configuration. Real implementation requires kafka-clients dependency. -->
        <!--
        <Kafka name="Kafka" topic="auth-logs">
            <PatternLayout pattern="%m" />
            <Property name="bootstrap.servers">kafka-broker1:9092,kafka-broker2:9092</Property>
        </Kafka>
        -->

    </Appenders>
    <Loggers>
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="JsonFile"/>
            <!-- <AppenderRef ref="Kafka"/> -->
        </Root>
        <Logger name="com.example.auth" level="debug" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="JsonFile"/>
            <!-- <AppenderRef ref="Kafka"/> -->
        </Logger>
    </Loggers>
</Configuration>
src/test/java/com/example/auth/api/AuthenticationController_LCVAPI_119Test.java