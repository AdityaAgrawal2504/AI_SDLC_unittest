<?xml version="1.0" encoding="UTF-8"?>
<Configuration>
    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%style{%d{ISO8601}}{black} %highlight{%-5level }[%style{%t}{bright,blue}] %style{%C{1.}}{bright,yellow}: %msg%n%throwable" />
        </Console>
        
        <RollingFile name="RollingFile"
                     fileName="./logs/user-registration-api.log"
                     filePattern="./logs/user-registration-api-%d{yyyy-MM-dd}-%i.log.gz">
            <JsonLayout complete="false" compact="true">
                 <KeyValuePair key="timestamp" value="$${date:yyyy-MM-dd'T'HH:mm:ss.SSS'Z'}"/>
            </JsonLayout>
            <Policies>
                <SizeBasedTriggeringPolicy size="10 MB"/>
                <TimeBasedTriggeringPolicy/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>
        
        <!-- Placeholder for Kafka Appender -->
        <!--
        <Kafka name="KafkaAppender" topic="logs-topic">
            <PatternLayout pattern="%d %p %c{1.} [%t] %m%n" />
            <Property name="bootstrap.servers">kafka-broker:9092</Property>
        </Kafka>
        -->
        
        <!-- Placeholder for Azure Event Hub Appender -->
        <!-- You would typically use a custom appender or a library like log4j2-azure-appender -->

    </Appenders>

    <Loggers>
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
            <!-- <AppenderRef ref="KafkaAppender"/> -->
        </Root>

        <Logger name="com.auth.api" level="debug" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
        </Logger>

    </Loggers>
</Configuration>
```
```java
// src/main/java/com/auth/api/UserRegistrationApiApplication_33A1.java