<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <Appenders>
        <Console name="ConsoleAppender" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
        </Console>

        <!-- File Appender for structured JSON logging -->
        <RollingFile name="FileAppender" fileName="logs/fetch-messages-api.log"
                     filePattern="logs/fetch-messages-api-%d{yyyy-MM-dd}-%i.log.gz">
            <JsonLayout complete="false" compact="true" eventEol="true">
                <KeyValuePair key="timestamp" value="$${date:yyyy-MM-dd'T'HH:mm:ss.SSS'Z'}"/>
            </JsonLayout>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>

        <!-- Abstracted Kafka Appender (example, requires kafka-log4j2-appender dependency) -->
        <!--
        <Kafka name="KafkaAppender" topic="api-logs">
            <PatternLayout pattern="%m" />
            <Property name="bootstrap.servers">kafka-broker:9092</Property>
            <JsonLayout complete="false" compact="true" eventEol="true">
                 <KeyValuePair key="timestamp" value="$${date:yyyy-MM-dd'T'HH:mm:ss.SSS'Z'}"/>
            </JsonLayout>
        </Kafka>
        -->
    </Appenders>

    <Loggers>
        <Root level="info">
            <AppenderRef ref="ConsoleAppender"/>
            <AppenderRef ref="FileAppender"/>
            <!-- <AppenderRef ref="KafkaAppender" /> -->
        </Root>

        <Logger name="com.fetchmessagesapi" level="debug" additivity="false">
            <AppenderRef ref="ConsoleAppender"/>
            <AppenderRef ref="FileAppender"/>
        </Logger>
    </Loggers>
</Configuration>
```
```java
// FILENAME: src/main/java/com/fetchmessagesapi/FetchMessagesApiApplicationFMA1.java